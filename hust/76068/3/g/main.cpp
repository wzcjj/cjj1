#include<iostream>
#include<cstdio>
#include<string>
#include<cstring>
#include<algorithm>
#include<vector>
#include <map>
map<string,int> m;
using namespace std;

int main()
{
    int T,t1,t2,i,j,k;
    char t[1050];
    scanf("%d\n",T);
    for (int i=0;i<T;i++)
    {
        gets(t);
        while (t[0]=='\n')
        {
            sscanf(t,"%[^:]s");
            i=strlen(t);
            while (sscanf())
        }
    }
    return 0;
}
